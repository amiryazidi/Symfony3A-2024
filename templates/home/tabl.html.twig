

{{ dump (user) }}

 {% for i in user %}
     {{i.name}}
     {{ i.age }}

{%  endfor %}

<br>
{# sans boucle #}

  {{ user[0].name }}
  {{ user[0].age }}
<br>
{# condition #}
  {% for u in user %}
            {% if u.age > 22 %}
                <tr>
                    <td>{{ u.name }}</td>
                    <td>{{ u.age }}</td>
                </tr>
            {% endif %}
{% endfor %}

 <br> 

 {# condition #}
  {% for u in user %}
  <td>{{ u.name }}</td>
  <td>{{ u.age }}</td>
  <img src="{{ u.image }}" alt="image" style="width:50px;height:50px;"/> 

    {% if u.age > 22 %}
        <input type="submit" value="register">
    {% endif %}    
    <a href="{{path('d',{'id':u.name})}}"> details </a>
 {% endfor %}